[project]
name = "mlflow-practice"
version = "0.1.0"
dependencies = [
    "mlflow",
    "scikit-learn",
    "numpy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]